# `@eldarlabs/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @eldarlabs/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@eldarlabs/prettier-config"
}
```
